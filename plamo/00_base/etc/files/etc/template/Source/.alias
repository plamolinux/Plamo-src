alias	h		history
alias	rmdvi		'rm *.dvi *.log *.aux *.toc'
alias	rmback		'rm *~ *.bak'
alias	j		jobs -l
alias	la		ls -a
alias	ll		ls -l
alias	z		suspend
alias	x		exit
alias	pd		pushd
alias	pd2		pushd +2
alias	pd3		pushd +3
alias	pd4		pushd +4
alias	df		df -k
alias	du		du -k
alias	tset		'set noglob histchars=""; eval `\tset -s \!*`; unset noglob histchars'
alias	LS		'ls -alF | less -E'
alias	mu		mule -rv
#alias	ssh1		'ssh -1'
#alias	scp1		'scp -oProtocol=1'
#alias	xv		'xv -nolimits'

