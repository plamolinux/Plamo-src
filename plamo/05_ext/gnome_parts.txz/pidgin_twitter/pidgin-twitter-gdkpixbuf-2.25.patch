diff -u -r pidgin-twitter-0.9.2.1.orig/icon.c pidgin-twitter-0.9.2.1/icon.c
--- pidgin-twitter-0.9.2.1.orig/icon.c	2011-05-13 00:08:01.000000000 +0900
+++ pidgin-twitter-0.9.2.1/icon.c	2012-02-10 12:36:43.734334202 +0900
@@ -280,7 +280,7 @@
     }
 
     if(data && data->pixbuf) {
-        gdk_pixbuf_unref(data->pixbuf);
+        g_object_unref(data->pixbuf);
         data->pixbuf = NULL;
     }
 
@@ -327,7 +327,7 @@
         size = DEFAULT_ICON_SIZE;
 
     dest = gdk_pixbuf_scale_simple(src, size, size, GDK_INTERP_HYPER);
-    gdk_pixbuf_unref(src);
+    g_object_unref(src);
 
     return dest;
 }
